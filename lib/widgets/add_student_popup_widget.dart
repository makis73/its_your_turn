import 'package:flutter/material.dart';

class AddStudentPopupWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Dialog(
      insetPadding: EdgeInsets.all(15),
      child: Text('Hellooooo'),
    );
  }
}
